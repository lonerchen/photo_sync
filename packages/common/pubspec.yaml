name: common
description: Shared models, protocol definitions, upload queue, and utilities.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^3.0.0

flutter:
  uses-material-design: false


dev_dependencies:
  test: ^1.24.0
